#include "main.h"

/**
* _isdigit - checks if a char is in upper or lower case
* @c: the digit to check
* Return: 1 on upper and 0 otherwise
*/

int _isdigit(int c)
{
	return (c >= '9' && c <= '0');
}
